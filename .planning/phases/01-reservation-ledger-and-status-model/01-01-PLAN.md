---
phase: 01-reservation-ledger-and-status-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260429000000_reservation_ledger_foundation.sql"
  - "utils/reservations/money.ts"
autonomous: true
requirements:
  - "LEDG-01"
  - "LEDG-02"
  - "LEDG-04"
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05: policy amounts are represented in integer minor units with currency and a snapshotted 5% platform fee rate."
    - "D-06/D-07/D-08/D-09: pickup window start/end, cancellation cutoff, policy version, policy snapshot, and checkout snapshot are stored durably on the reservation foundation."
    - "LEDG-04: financial ledger rows are append-only and visibility-aware for later client, producer, and internal history without direct exposure of internal payment tables."
  artifacts:
    - "supabase/migrations/20260429000000_reservation_ledger_foundation.sql"
    - "utils/reservations/money.ts"
  key_links:
    - "Schema money columns use the same constants exported by utils/reservations/money.ts: PLATFORM_FEE_BPS=500, LATE_CANCEL_OR_NO_SHOW_BPS=2000, DEFAULT_RESERVATION_CURRENCY=\"CAD\"."
---

<objective>
Create the Phase 1 schema and helper foundation for authoritative reservation money, immutable checkout/policy snapshots, and append-only financial ledger history.

Purpose: Later checkout, webhook, pickup, cancellation, no-show, refund, transfer, and dashboard work need one durable model for money and reservation snapshots before they mutate live financial state.
Output: One corrective Supabase migration and one pure TypeScript money helper module.
</objective>

<execution_context>
@/Users/mikaelboutin/Desktop/coding-projects/kiosq-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mikaelboutin/Desktop/coding-projects/kiosq-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reservation-ledger-and-status-model/01-CONTEXT.md
@.planning/phases/01-reservation-ledger-and-status-model/01-RESEARCH.md
@.planning/phases/01-reservation-ledger-and-status-model/01-PATTERNS.md
@.planning/phases/01-reservation-ledger-and-status-model/01-UI-SPEC.md
@supabase/migrations/20250713000000_create_orders_reservations.sql
@supabase/migrations/20250714000000_update_reservations_orders_workflow.sql
@supabase/migrations/20250714000001_remove_reservation_columns.sql
@package.json
</context>

<threat_model>
  <threat id="T-01-01" severity="high">
    <risk>Money unit drift creates incorrect client charges, refund amounts, platform fees, or producer compensation.</risk>
    <mitigation>Use BIGINT *_minor columns, uppercase currency checks, and pure helper constants/calculations. Keep legacy NUMERIC fields non-authoritative.</mitigation>
    <verification>`rg "BIGINT|amount_minor|currency|PLATFORM_FEE_BPS|LATE_CANCEL_OR_NO_SHOW_BPS" supabase/migrations/20260429000000_reservation_ledger_foundation.sql utils/reservations/money.ts`</verification>
  </threat>
  <threat id="T-01-02" severity="high">
    <risk>Mutable product, kiosq, schedule, or policy data rewrites the accepted reservation terms after payment.</risk>
    <mitigation>Store typed pickup/cutoff columns plus immutable `checkout_snapshot` and `policy_snapshot` JSONB records.</mitigation>
    <verification>`rg "checkout_snapshot|policy_snapshot|pickup_window_start|cancellation_cutoff_at" supabase/migrations/20260429000000_reservation_ledger_foundation.sql`</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add money, snapshot, payment, settlement, and ledger schema foundation</name>
  <files>supabase/migrations/20260429000000_reservation_ledger_foundation.sql</files>
  <read_first>supabase/migrations/20250713000000_create_orders_reservations.sql, supabase/migrations/20250714000000_update_reservations_orders_workflow.sql, supabase/migrations/20250714000001_remove_reservation_columns.sql, .planning/phases/01-reservation-ledger-and-status-model/01-CONTEXT.md</read_first>
  <action>Create `supabase/migrations/20260429000000_reservation_ledger_foundation.sql` with `BEGIN;` and `COMMIT;`. Add reservation snapshot columns with `ALTER TABLE reservations ADD COLUMN IF NOT EXISTS`: `pickup_window_start TIMESTAMPTZ`, `pickup_window_end TIMESTAMPTZ`, `cancellation_cutoff_at TIMESTAMPTZ`, `policy_version TEXT NOT NULL DEFAULT 'kiosq-policy-v1'`, `policy_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb`, `checkout_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb`, `status_reason TEXT`, `status_changed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, and `status_changed_by UUID REFERENCES users(id) ON DELETE SET NULL`. Add order money columns with `ALTER TABLE orders ADD COLUMN IF NOT EXISTS`: `total_amount_minor BIGINT`, `currency TEXT NOT NULL DEFAULT 'CAD'`, and `platform_fee_bps INTEGER NOT NULL DEFAULT 500`; add checks named `orders_total_amount_minor_non_negative`, `orders_currency_uppercase_three_letter`, and `orders_platform_fee_bps_range`. Add order item money snapshot columns `unit_price_minor BIGINT`, `total_price_minor BIGINT`, and `currency TEXT NOT NULL DEFAULT 'CAD'` with non-negative/currency checks. Create `reservation_financial_summaries` with `reservation_id UUID PRIMARY KEY REFERENCES reservations(id) ON DELETE CASCADE`, optional `order_id UUID UNIQUE REFERENCES orders(id) ON DELETE SET NULL`, `currency TEXT NOT NULL DEFAULT 'CAD'`, `client_paid_amount_minor BIGINT NOT NULL DEFAULT 0`, `client_refund_amount_minor BIGINT NOT NULL DEFAULT 0`, `client_retained_amount_minor BIGINT NOT NULL DEFAULT 0`, `platform_fee_amount_minor BIGINT NOT NULL DEFAULT 0`, `producer_compensation_amount_minor BIGINT NOT NULL DEFAULT 0`, `stripe_processing_fee_amount_minor BIGINT NOT NULL DEFAULT 0`, `platform_fee_bps INTEGER NOT NULL DEFAULT 500`, JSONB `calculation_snapshot`, timestamps, and checks that all amount columns are non-negative. Create `reservation_payment_intents`, `reservation_settlements`, `reservation_refunds`, `reservation_transfers`, and `reservation_ledger_entries` tables with UUID primary keys, `reservation_id`, amount/currency fields, status fields, `idempotency_key TEXT`, timestamps, and JSONB metadata. `reservation_payment_intents` must include `stripe_payment_intent_id`, `stripe_charge_id`, `transfer_group`, `amount_minor`, `currency`, and `status`. `reservation_settlements` must include `settlement_reason` values `pickup_confirmed`, `early_cancelled`, `late_cancelled`, `no_show`, `admin_review`, the D-05 amount fields, `platform_fee_bps`, `policy_version`, `policy_snapshot`, and `status`. `reservation_refunds` must include `settlement_id`, `stripe_refund_id`, `refund_type`, `amount_minor`, `currency`, `status`, and `idempotency_key`. `reservation_transfers` must include `settlement_id`, `stripe_transfer_id`, `transfer_type`, `source_charge_id`, `destination_stripe_account_id`, `amount_minor`, `currency`, `status`, and `idempotency_key`. `reservation_ledger_entries` must include `entry_type`, `amount_minor`, `currency`, `visibility TEXT[] NOT NULL DEFAULT ARRAY['internal']::TEXT[]`, `idempotency_key TEXT`, `actor_type`, `actor_id`, `occurred_at`, and JSONB metadata. Enable RLS on every new table. For `reservation_ledger_entries`, add visibility-scoped SELECT policies: customers can read only rows where they own the reservation and `'client' = ANY(visibility)`, producers can read only rows where they own the reservation's `vendor_profile_id` and `'producer' = ANY(visibility)`. Do not add direct client/producer SELECT policies on `reservation_financial_summaries`, `reservation_payment_intents`, `reservation_settlements`, `reservation_refunds`, or `reservation_transfers`; later dashboard APIs/RPCs must expose role-safe DTOs instead of raw financial tables. Do not add broad UPDATE policies for ledger tables. Reference D-01 through D-09 and D-16/D-17 in SQL comments near the relevant schema/RLS sections.</action>
  <verify>
    <automated>rg "pickup_window_start|cancellation_cutoff_at|policy_snapshot|checkout_snapshot|reservation_financial_summaries|reservation_payment_intents|reservation_settlements|reservation_refunds|reservation_transfers|reservation_ledger_entries|client_retained_amount_minor|producer_compensation_amount_minor" supabase/migrations/20260429000000_reservation_ledger_foundation.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260429000000_reservation_ledger_foundation.sql` contains `BEGIN;` and `COMMIT;`.
    - The migration contains `ADD COLUMN IF NOT EXISTS pickup_window_start TIMESTAMPTZ`.
    - The migration contains `ADD COLUMN IF NOT EXISTS cancellation_cutoff_at TIMESTAMPTZ`.
    - The migration contains `policy_version TEXT NOT NULL DEFAULT 'kiosq-policy-v1'`.
    - The migration contains `CREATE TABLE IF NOT EXISTS reservation_financial_summaries`.
    - The migration contains `client_paid_amount_minor`, `client_refund_amount_minor`, `client_retained_amount_minor`, `platform_fee_amount_minor`, and `producer_compensation_amount_minor`.
    - The migration contains `CREATE TABLE IF NOT EXISTS reservation_ledger_entries`.
    - The migration contains `visibility TEXT[] NOT NULL DEFAULT ARRAY['internal']::TEXT[]`.
    - The migration contains `ALTER TABLE reservation_ledger_entries ENABLE ROW LEVEL SECURITY`.
    - The migration contains `'client' = ANY(visibility)`.
    - The migration contains `'producer' = ANY(visibility)`.
  </acceptance_criteria>
  <done>Money, snapshot, payment, settlement, refund, transfer, and ledger schema foundations exist in one new migration with RLS and grep-verifiable D-01 through D-09 references.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared reservation money and policy calculation helpers</name>
  <files>utils/reservations/money.ts</files>
  <read_first>actions/create-reservation.ts, actions/create-reservation-payment-intent.ts, app/api/payment-success/route.ts, .planning/phases/01-reservation-ledger-and-status-model/01-CONTEXT.md</read_first>
  <action>Create `utils/reservations/money.ts` with named exports only. Export `DEFAULT_RESERVATION_CURRENCY = "CAD"`, `SUPPORTED_RESERVATION_CURRENCIES = ["CAD", "USD"] as const`, `PLATFORM_FEE_BPS = 500`, `LATE_CANCEL_OR_NO_SHOW_BPS = 2000`, type `ReservationCurrency`, type `ReservationPolicyAmounts`, and functions `toMinorUnits(amount: number): number`, `multiplyMinorUnits(unitAmountMinor: number, quantity: number): number`, `calculateBpsAmount(amountMinor: number, bps: number): number`, and `calculateLateCancellationAmounts(orderTotalMinor: number, currency: ReservationCurrency = DEFAULT_RESERVATION_CURRENCY): ReservationPolicyAmounts`. `calculateLateCancellationAmounts` must return `clientPaidAmountMinor`, `clientRefundAmountMinor`, `clientRetainedAmountMinor`, `platformFeeAmountMinor`, `producerCompensationAmountMinor`, `currency`, `platformFeeBps`, and `policyPenaltyBps`. Implement D-01 by calculating retained amount as 20% of the order total. Implement D-02 and D-03 by calculating platform fee as 5% of the retained amount, and producer compensation as retained amount minus platform fee. Implement D-04 by setting the refund amount to order total minus retained amount for late cancellation/no-show; early cancellation full-refund behavior is represented by the zero-retained branch used by later policy actions. Throw `Error` for non-finite, negative, or unsafe integer inputs. Keep display formatting out of this module.</action>
  <verify>
    <automated>rg "DEFAULT_RESERVATION_CURRENCY|SUPPORTED_RESERVATION_CURRENCIES|PLATFORM_FEE_BPS|LATE_CANCEL_OR_NO_SHOW_BPS|calculateLateCancellationAmounts|producerCompensationAmountMinor" utils/reservations/money.ts</automated>
  </verify>
  <acceptance_criteria>
    - `utils/reservations/money.ts` contains `export const DEFAULT_RESERVATION_CURRENCY = "CAD";`.
    - `utils/reservations/money.ts` contains `export const PLATFORM_FEE_BPS = 500;`.
    - `utils/reservations/money.ts` contains `export const LATE_CANCEL_OR_NO_SHOW_BPS = 2000;`.
    - `utils/reservations/money.ts` exports `calculateLateCancellationAmounts`.
    - `calculateLateCancellationAmounts` returns `producerCompensationAmountMinor`.
    - The file does not import React, Stripe, Supabase, or Next.js modules.
  </acceptance_criteria>
  <done>Reservation money and policy math have a pure helper module with constants that match the database foundation and D-01 through D-05.</done>
</task>

<task type="auto">
  <name>Task 3: Verify schema/helper foundation coverage</name>
  <files>supabase/migrations/20260429000000_reservation_ledger_foundation.sql, utils/reservations/money.ts</files>
  <read_first>.planning/phases/01-reservation-ledger-and-status-model/01-VALIDATION.md</read_first>
  <action>Run the static verification commands for Plan 01. Confirm the migration and helper contain the exact table names, amount fields, policy constants, and snapshot fields required by LEDG-01, LEDG-02, LEDG-04, D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, and D-09. Do not run `npm run db:push` in this plan; the blocking schema push runs after all Phase 1 migration files exist in Plan 03.</action>
  <verify>
    <automated>rg "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|reservation_financial_summaries|reservation_ledger_entries|calculateLateCancellationAmounts" supabase/migrations/20260429000000_reservation_ledger_foundation.sql utils/reservations/money.ts</automated>
  </verify>
  <acceptance_criteria>
    - The verification command exits 0.
    - `rg "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09" supabase/migrations/20260429000000_reservation_ledger_foundation.sql utils/reservations/money.ts` exits 0.
    - `rg "reservation_financial_summaries|reservation_ledger_entries" supabase/migrations/20260429000000_reservation_ledger_foundation.sql` exits 0.
  </acceptance_criteria>
  <done>Plan 01 artifacts are present and statically prove coverage for money, snapshots, policy amounts, and ledger foundations.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg "reservation_financial_summaries|reservation_ledger_entries|total_amount_minor|pickup_window_start" supabase/migrations/20260429000000_reservation_ledger_foundation.sql` exits 0.
- [ ] `rg "PLATFORM_FEE_BPS|LATE_CANCEL_OR_NO_SHOW_BPS|calculateLateCancellationAmounts" utils/reservations/money.ts` exits 0.
- [ ] No user-facing UI files are created or modified by this plan.
</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass.
- No new UI screens, checkout flows, QR flows, transfers, refunds, or dashboards are implemented.
- LEDG-01, LEDG-02, and LEDG-04 have concrete schema/helper artifacts ready for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reservation-ledger-and-status-model/01-01-SUMMARY.md`.
</output>
